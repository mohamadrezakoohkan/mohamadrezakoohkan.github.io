# Project Architecture Diagram

## Layer Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                       │
│                        (index.html)                          │
│  - Semantic HTML structure                                   │
│  - No inline styles (except CSS variable overrides)          │
│  - No business logic                                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      STYLING LAYER                           │
│                     (assets/css/styles.css)                  │
│  - CSS Variables (theming)                                   │
│  - BEM-style classes                                         │
│  - Reusable utility classes                                  │
│  - DOS component styles                                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   ORCHESTRATION LAYER                        │
│                     (assets/js/main.js)                      │
│  - Initializes all modules                                   │
│  - Coordinates startup sequence                              │
│  - No business logic                                         │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ▼             ▼             ▼
┌──────────────────┐ ┌──────────────┐ ┌──────────────┐
│  GREETING MODULE │ │ CONTENT      │ │  EFFECTS     │
│  (greeting.js)   │ │ MODULE       │ │  MODULE      │
│                  │ │ (content-    │ │  (effects.js)│
│ ┌──────────────┐ │ │  loader.js)  │ │              │
│ │GreetingService│ │ │              │ │ ┌──────────┐ │
│ │  IPService   │ │ │ ┌──────────┐ │ │ │ Sparkle  │ │
│ │GreetingRender│ │ │ │Content   │ │ │ │ Effect   │ │
│ └──────────────┘ │ │ │Loader    │ │ │ │ Cursor   │ │
└──────────────────┘ │ └──────────┘ │ │ │ Sparkle  │ │
                     └──────────────┘ │ └──────────┘ │
                              │       └──────────────┘
                              ▼
                     ┌──────────────┐
                     │  RENDERER    │
                     │  MODULE      │
                     │ (renderer.js)│
                     │              │
                     │ ┌──────────┐ │
                     │ │DOMRender │ │
                     │ │PostFactor│ │
                     │ │LinkFactor│ │
                     │ └──────────┘ │
                     └──────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    CONFIGURATION LAYER                       │
│                   (assets/js/constants.js)                   │
│  - SELECTORS (DOM IDs)                                       │
│  - CSS_CLASSES (class names)                                 │
│  - COLORS (color palette)                                    │
│  - API (endpoints)                                           │
│  - DATA_PATHS (JSON files)                                   │
│  - ANIMATION (timings)                                       │
│  - DEFAULTS (fallback values)                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        DATA LAYER                            │
│                      (data/*.json)                           │
│  - content.json (site metadata)                              │
│  - posts.json (blog posts)                                   │
│  - links.json (sidebar links)                                │
│  - greetings.json (timezone greetings)                       │
└─────────────────────────────────────────────────────────────┘
```

## Module Dependencies

```
main.js
  │
  ├─► greeting.js
  │     ├─► constants.js (SELECTORS, API, DATA_PATHS, DEFAULTS, COLORS)
  │     └─► data/greetings.json
  │
  ├─► content-loader.js
  │     ├─► constants.js (DATA_PATHS)
  │     ├─► renderer.js
  │     ├─► data/content.json
  │     ├─► data/posts.json
  │     └─► data/links.json
  │
  ├─► renderer.js
  │     └─► constants.js (SELECTORS, CSS_CLASSES, COLORS)
  │
  └─► effects.js
        └─► constants.js (ANIMATION, SPARKLE_COLORS)
```

## Data Flow

```
User Loads Page
      │
      ▼
┌──────────────┐
│  index.html  │ ──► Loads jQuery
└──────────────┘
      │
      ▼
┌──────────────┐
│ constants.js │ ──► Defines all constants
└──────────────┘
      │
      ▼
┌──────────────┐
│   main.js    │ ──► Orchestrates initialization
└──────────────┘
      │
      ├─► initIPGreeting()
      │     │
      │     ├─► GreetingService.loadGreetingsData()
      │     │     └─► Fetch data/greetings.json
      │     │
      │     ├─► IPService.fetchIP()
      │     │     └─► Fetch from API
      │     │
      │     └─► GreetingRenderer.render()
      │           └─► Update #greeting-banner
      │
      ├─► loadContent()
      │     │
      │     ├─► ContentLoader.loadJSON() × 3
      │     │     ├─► data/content.json
      │     │     ├─► data/posts.json
      │     │     └─► data/links.json
      │     │
      │     └─► Call render functions
      │           ├─► renderSiteTitle()
      │           ├─► renderSidebarAbout()
      │           ├─► renderFooter()
      │           ├─► renderSidebarLinks()
      │           └─► renderPosts()
      │                 │
      │                 └─► PostElementFactory.createPostElement()
      │                       └─► DOMRenderer.appendToContainer()
      │
      ├─► initSparkleEffect()
      │     └─► SparkleEffect.start()
      │
      └─► initCursorSparkles()
            └─► CursorSparkleEffect.start()
```

## SOLID Principles Mapping

```
┌─────────────────────────────────────────────────────────────┐
│ Single Responsibility Principle (SRP)                        │
├─────────────────────────────────────────────────────────────┤
│ constants.js     → Configuration only                        │
│ greeting.js      → Greeting logic only                       │
│ content-loader.js → Data loading only                        │
│ renderer.js      → DOM rendering only                        │
│ effects.js       → Visual effects only                       │
│ styles.css       → Presentation only                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Open/Closed Principle (OCP)                                  │
├─────────────────────────────────────────────────────────────┤
│ PostElementFactory   → Can add new post types               │
│ LinkElementFactory   → Can add new link types               │
│ CSS Variables        → Can change theme without modifying   │
│ Service Objects      → Can extend with new methods          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Liskov Substitution Principle (LSP)                          │
├─────────────────────────────────────────────────────────────┤
│ All factories return jQuery objects (interchangeable)       │
│ All services follow consistent interfaces                   │
│ All render functions accept predictable data structures     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Interface Segregation Principle (ISP)                        │
├─────────────────────────────────────────────────────────────┤
│ DOMRenderer          → 3 focused methods                     │
│ GreetingService      → 3 specific methods                    │
│ IPService            → 1 clear method                        │
│ ContentLoader        → 3 minimal methods                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Dependency Inversion Principle (DIP)                         │
├─────────────────────────────────────────────────────────────┤
│ All modules depend on constants.js (abstraction)            │
│ Functions accept callbacks (abstraction)                    │
│ jQuery provides DOM abstraction                             │
└─────────────────────────────────────────────────────────────┘
```

## Design Patterns Used

```
┌─────────────────────────────────────────────────────────────┐
│ Factory Pattern                                              │
├─────────────────────────────────────────────────────────────┤
│ PostElementFactory.createPostElement()                      │
│ PostElementFactory.createNewBadge()                         │
│ PostElementFactory.createPostTitle()                        │
│ PostElementFactory.createPostExcerpt()                      │
│ PostElementFactory.createReadMoreButton()                   │
│ LinkElementFactory.createLinkElement()                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Service Pattern                                              │
├─────────────────────────────────────────────────────────────┤
│ GreetingService (business logic)                            │
│ IPService (API calls)                                        │
│ ContentLoader (data loading)                                │
│ SparkleEffect (animation logic)                             │
│ CursorSparkleEffect (cursor logic)                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Renderer Pattern                                             │
├─────────────────────────────────────────────────────────────┤
│ DOMRenderer (generic DOM manipulation)                      │
│ GreetingRenderer (specific greeting rendering)              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Module Pattern                                               │
├─────────────────────────────────────────────────────────────┤
│ All JavaScript files expose clean public APIs               │
│ Private state encapsulated in service objects               │
└─────────────────────────────────────────────────────────────┘
```

## File Organization

```
Blog/
├── 📄 index.html                    (120 lines) - Structure
├── 📄 CLEAN_CODE_SUMMARY.md         (New) - Documentation
│
├── 📁 assets/
│   ├── 📁 css/
│   │   └── 📄 styles.css            (160 lines) - Presentation
│   │
│   └── 📁 js/
│       ├── 📄 constants.js          (62 lines) - Configuration
│       ├── 📄 greeting.js           (115 lines) - Greeting logic
│       ├── 📄 content-loader.js     (50 lines) - Data loading
│       ├── 📄 renderer.js           (180 lines) - DOM rendering
│       ├── 📄 effects.js            (120 lines) - Visual effects
│       └── 📄 main.js               (20 lines) - Orchestration
│
├── 📁 data/
│   ├── 📄 content.json              - Site metadata
│   ├── 📄 posts.json                - Blog posts
│   ├── 📄 links.json                - Sidebar links
│   └── 📄 greetings.json            - Timezone greetings
│
└── 📁 .cursor/
    └── 📁 plans/
        ├── 📄 clean-code-architecture.md  - Architecture guide
        └── 📄 architecture-diagram.md     - This file
```

## Complexity Metrics

```
Before Refactoring:
┌─────────────────┬──────────┬────────────┬───────────┐
│ File            │ Lines    │ Complexity │ Coupling  │
├─────────────────┼──────────┼────────────┼───────────┤
│ index.html      │ 199      │ Low        │ High      │
│ renderer.js     │ 105      │ High (8-12)│ High      │
│ greeting.js     │ 80       │ Medium     │ High      │
│ effects.js      │ 61       │ Medium     │ Medium    │
│ content-loader  │ 30       │ Low        │ High      │
└─────────────────┴──────────┴────────────┴───────────┘

After Refactoring:
┌─────────────────┬──────────┬────────────┬───────────┐
│ File            │ Lines    │ Complexity │ Coupling  │
├─────────────────┼──────────┼────────────┼───────────┤
│ index.html      │ 120      │ Low        │ Low       │
│ styles.css      │ 160      │ Low        │ None      │
│ constants.js    │ 62       │ None       │ None      │
│ renderer.js     │ 180      │ Low (1-3)  │ Low       │
│ greeting.js     │ 115      │ Low (1-3)  │ Low       │
│ effects.js      │ 120      │ Low (1-3)  │ Low       │
│ content-loader  │ 50       │ Low (1-2)  │ Low       │
│ main.js         │ 20       │ Low        │ Low       │
└─────────────────┴──────────┴────────────┴───────────┘

Result: More lines, but MUCH better quality! ✨
```

## Testing Strategy (Future)

```
Unit Tests:
├── constants.test.js      (Test constant values)
├── greeting.test.js       (Test GreetingService, IPService)
├── content-loader.test.js (Test ContentLoader)
├── renderer.test.js       (Test factories and DOMRenderer)
└── effects.test.js        (Test effect services)

Integration Tests:
├── main.integration.test.js (Test module coordination)
└── rendering.integration.test.js (Test full render flow)

E2E Tests:
└── user-flow.e2e.test.js (Test complete user experience)
```
